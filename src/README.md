### Source for Hellhouse Minion

`src/commands` => Commands for the bot
`src/database` => Database functions / storage methods
`src/logger` => Logger used throughout the bot
`src/events` => Event handlers